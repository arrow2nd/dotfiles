[tools]
node = "24"
bun = "1"
usage = "latest"
yarn = "4"
pnpm = "9"
rust = "latest"

[settings]
idiomatic_version_file_enable_tools = ["node"]

# macOSではデフォルトでこれだけど、Linuxではそうではないので明示的に。NixOS用
[settings.node]
compile = false
